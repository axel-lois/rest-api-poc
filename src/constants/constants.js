const VALID_STATUSES = ['TO_DO', 'IN_PROGRESS', 'DONE']; // move to another folder, something like constants 

const MESSAGES = {
    API_RUNNING: 'API is running!',
}

module.exports = {
    VALID_STATUSES,
    MESSAGES
}